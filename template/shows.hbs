<!DOCTYPE html>
<html lang="en">
<head>
    {{>header}}
</head>
<body>
    {{>navbar}}
    <main>
        {{#if shows}}
            {{#each shows}}
                <div class="show">
                        <div class="row">
                            <img class="poster" src="{{this.movie.posterUrl}}" alt="{{this.movie.posterUrl}}"/>
                            <div class="show-info">
                                <a href="/movie/{{this.movie.id}}"><h2>{{this.movie.title}}</h2></a>
                                <a href="/book_show/{{this.id}}">
                                    <div class="row">
                                        <h4>{{this.price}}$</h4>
                                            
                                        <h4>{{this.time}}</h4>
                                    </div>
                                </a> 
                            </div>
                        </div>
                </div>
            {{/each}}
        {{else}}
            Brak seansów
        {{/if}}
    </main>
    {{>footer}}
</body>
</html>